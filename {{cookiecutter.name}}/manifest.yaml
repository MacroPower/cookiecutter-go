features:
  - name: vscode
    enabled: '{{ cookiecutter.vscode|lower }}'
    resources:
      - .devcontainer
      - .vscode

  - name: gh_actions
    enabled: '{{ cookiecutter.gh_actions|lower }}'
    resources:
      - .github/workflows
